	# Generate new ssh key
ssh-keygen -t ed25519 -C "email@email.com"

	# Add SSH key to github
	user -> settings -> copy and paste ssh key

	# Touch server
ssh -T git@github.com

	# Clone Bases
git clone git@github.com:username/directory
